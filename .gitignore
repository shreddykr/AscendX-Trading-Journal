node_modules/

# Local user data — private, never committed
data/
database.json

# Environment / secrets
.env

# Cloudflare Tunnel (client binary + private connector token)
cloudflared.exe
cloudflared.token
